# Secure Password Generator

A simple and robust CLI tool to generate cryptographically strong passwords.

## Features
- **Fast to code:** Written in clean Python.
- **Secure:** Uses the `secrets` module for high-quality randomness.
- **Type-safe:** Uses Python type hints for better developer experience.

## Usage
Run the script and enter the desired password length:
```bash
python main.py
